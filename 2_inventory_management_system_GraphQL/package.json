{
  "name": "assigment3",
  "version": "1.0.0",
  "description": "Full Stack - Module 3 and 4 (React class amd state)",
  "main": "server/server.js",
  "scripts": {
    "start": "nodemon -w server -e js,graphql server/server.js",
    "watch": "babel src --presets @babel/react --out-dir public --watch --verbose",
    "compile": "babel src --presets @babel/react --out-dir public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apollo-server-express": "^2.10.1",
    "express": "^4.17.1",
    "graphql": "^0.13.2",
    "nodemon": "^2.0.2"
  },
  "devDependencies": {
    "@babel/cli": "^7.8.4",
    "@babel/core": "^7.8.6",
    "@babel/preset-env": "^7.8.6",
    "@babel/preset-react": "^7.8.3"
  }
}
